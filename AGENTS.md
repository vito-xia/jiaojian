# AI 协作入口

本文件适用于整个仓库。任何新 AI 会话在修改代码、数据或文档前，都必须先执行本文件的冷启动流程。

## 1. 强制冷启动

1. 完整读取根目录的 `项目交接文档.md`，不得只依据聊天摘要或历史对话继续开发。
2. 执行 `git status --short` 和 `git rev-parse --short HEAD`，确认现有改动与交接文档记录的代码基线。
3. 保留所有用户或其他协作者已有改动；不清理、不覆盖、不回退无关文件。
4. 根据任务类型，只读取交接文档指定章节和相关源码，避免把历史聊天重新引入为默认上下文。
5. 如果文档、当前代码和最新用户要求不一致，先区分“预期规则”和“实际行为”；存在会改变业务结果的歧义时，向用户说明后再决定。

## 2. 信息权威顺序

判断项目行为时按以下顺序：

1. 用户在当前任务中的最新明确要求。
2. `项目交接文档.md` 中标记为“已确认/必须保持”的当前规则。
3. 当前代码、生成数据和页面所体现的实际行为。
4. `README.md`。
5. 历史聊天、旧截图或未写入交接文档的记忆。

历史聊天只用于解释背景，不得覆盖交接文档中的当前有效结论。文档与代码冲突时，不可静默选择其一：代码说明现状，文档说明意图，应结合 Git 差异核对；仍影响业务取舍时再请求用户确认。

## 3. 必须保持的项目边界

- 未收到重新设计要求时，保持现有视觉语言、模块层级、信息密度、列顺序、分页、抽屉和响应式行为。
- 当前 `dashboard.html`、`assets/dashboard.css` 和 `assets/dashboard.js` 的实际渲染结果是视觉与交互基线；不得为了“更现代”或“更简洁”擅自重做。
- 保持原生 HTML/CSS/JavaScript/Canvas 技术栈和 `file://` 离线可运行能力；不得擅自迁移框架、增加 CDN 或业务接口依赖。
- 保持 `dashboard_bundle.js + dashboard-loader.js + dashboard_*.js` 的数据分片机制。
- `data/`、`dist/`、`site-deploy/public/` 和 Worker 包属于生成物或发布副本，不直接作为业务源码修改。
- 业务计算改在 `process_data.py`；页面结构改在 `dashboard.html`；渲染与交互改在 `assets/dashboard.js`；样式改在 `assets/dashboard.css`。
- 必须区分真实数值 0 与空值。0 分、0 票、0% 不能被转换为 `—`；缺失值不能被伪造成 0。
- 不得顺手修复 `项目交接文档.md` 中标记为“待确认”或“待授权”的事项。
- 不修改 `数据源/链接清单.txt` 中的访问地址，不在输出中暴露可能带权限的链接。
- 不使用 `_dl_and_process.py`；它是已停用的一次性历史脚本。
- 未经用户明确要求，不提交、不推送 Git，不发布项目，也不修改外部部署状态。

## 4. 按任务最小读取范围

| 任务类型 | 先读交接文档 | 主要源码 |
|---|---|---|
| 高停滞积分、NEW、扣分量级、最近一天指标 | `7.3`、`7.8` | `process_data.py`、`dashboard.html`、`assets/dashboard.js` |
| 平台管控、清退、极差熔断 | `7.4`、`7.5` | `process_data.py`、`dashboard.html`、`assets/dashboard.js`、`assets/dashboard.css` |
| 交件预警或平台差异 | `7.1`、`7.2` | `process_data.py`、`assets/dashboard.js` |
| 网点抽屉、趋势、历史缺货 | `7.6`、`8` | `process_data.py`、`assets/dashboard.js` |
| 派送积分或派送管控 | `7.7`、`12` | `process_data.py`、`dashboard.html`、`assets/dashboard.js` |
| 页面布局、列宽、响应式、视觉 | `8`、`10.3` | `dashboard.html`、`assets/dashboard.css`、`assets/dashboard.js` |
| 数据源或字段变化 | `5`、`6`、`10.2` | `process_data.py`、对应 Excel 表结构 |
| 数据更新 | `5`、`6`、`9`、`10` | `process_data.py`、`数据源/`、`data_quality_report.json` |
| 加载性能或数据分片 | `6.3`、`8` | `assets/dashboard-loader.js`、`assets/dashboard.js`、`process_data.py` |
| 构建或发布 | `3.1`、`9`、`11` | `build.mjs`、`package.json`、`site-deploy/` |
| 仅修改文档 | `0`、`15` | `AGENTS.md`、`项目交接文档.md`、必要时 `README.md` |

## 5. 修改流程

1. 先确认当前行为、任务范围及工作树，不扩大修改范围。
2. 做最小且完整的改动；表格字段变化通常要同步检查 Python 输出、HTML 表头、JavaScript 渲染和 CSS。
3. 只有 `process_data.py`、数据源或数据契约变化时才重新生成 `data/`；纯文档或纯样式调整不应无故刷新大量数据。
4. 修改静态 CSS/JS 后，检查 `dashboard.html` 中对应查询参数是否需要递增，避免缓存旧资源。
5. 只有发布任务或需要验证发布产物时，才执行 `npm run build` 和 `npm run build:worker`。
6. 按任务风险运行验证，并检查 `git status --short`，确保没有混入无关文件。
7. 若已确认的业务口径、视觉契约、数据字段、构建方式或已知问题状态发生变化，同步更新 `项目交接文档.md`。

## 6. 最低验证要求

| 修改范围 | 最低验证 |
|---|---|
| `process_data.py` | `python -m py_compile process_data.py`；重新生成数据；核对质量报告和边界样例 |
| `assets/dashboard.js` 或加载器 | `node --check`；浏览器控制台；相关平台、日期、分页、搜索、抽屉回归 |
| `dashboard.html` / `dashboard.css` | 桌面端和窄屏回归；确认模块顺序、表格滚动、分页与抽屉没有漂移 |
| 数据源更新 | 核对 `as_of`、行数、排除量、未匹配网点和当前样例 |
| 构建/发布 | `npm run build`；按需执行 Worker 构建；确认根目录、`dist/` 与发布包版本一致 |
| 仅文档 | 检查 Markdown 围栏、相对链接、编码、标题结构和 Git 差异 |

验证应与修改范围成比例，但业务边界样例不能只用“脚本成功”代替。当前关键样例与验收项见交接文档 `10`。

## 7. 当前禁止擅自处理的事项

- 派送模块页面“15 天”与后端“16 天”口径不一致：等待业务确认。
- CSS 中的字面量换行转义残留：等待明确的修复任务，不在其他改动中顺手处理。
- 极差熔断固定取全局最近 20 条且不随顶部日期变化：这是当前已确认行为，不是缺陷。
- `数据源/` 与 `site-deploy/` 在 Git 中缺失时，应先判断是否因忽略规则导致；不得自行伪造源数据或部署配置。

## 8. 文档回写规则

- 交接文档维护“当前有效事实”，不复制完整聊天，不持续堆叠已经失效的历史要求。
- 新规则替代旧规则时，直接更新对应章节；只有解释仍有价值时才保留简短决策说明。
- 待确认事项必须写明状态和当前行为；未获确认不得把建议写成已实施结果。
- 数据日期、代码基线或质量指标变化时，更新交接文档 `2`。
- 任务完成时向用户说明：修改文件、验证结果、尚未解决或需要确认的事项。
